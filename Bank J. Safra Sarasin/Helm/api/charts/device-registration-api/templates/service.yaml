{{- if .Values.enabled -}}
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.global.namespace }}
  name: {{ include "device-registration-api.name" . }}
  labels:
    {{- include "device-registration-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "device-registration-api.selectorLabels" . | nindent 4 }}
{{- end }}
